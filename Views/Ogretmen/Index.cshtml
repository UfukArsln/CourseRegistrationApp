@model IEnumerable<Ogretmen>

    <h2>Öğretmen Listesi</h2>
    <hr>

    <a asp-controller="Ogretmen" asp-action="Create" class="btn btn-secondary">Öğretmen Kayıt</a>
    <hr>

    <table class="table border">
        <thead>
            <tr>
                <th style="width: 150px;">@Html.DisplayNameFor(m => m.OgretmenId)</th>
                <th>@Html.DisplayNameFor(m => m.Ad)</th>
                <th>@Html.DisplayNameFor(m => m.Soyad)</th>
                <th style="width: 200px;">@Html.DisplayNameFor(m =>m.BaslamaTarihi)</th>
                <th style="width: 150px;"></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.OgretmenId)</td>
                <td>@Html.DisplayFor(modelItem => item.Ad)</td>
                <td>@Html.DisplayFor(modelItem => item.Soyad)</td>
                <td>@Html.DisplayFor(modelItem => item.BaslamaTarihi)</td>
                <td>
                    <a asp-controller="Ogretmen" asp-action="Edit" asp-route-id="@item.OgretmenId"
                        class="btn btn-primary">Düzenle</a>
                    <a asp-controller="Ogretmen" asp-action="Delete" asp-route-id="@item.OgretmenId"
                        class="btn btn-danger">Sil</a>
                </td>
            </tr>
            }
        </tbody>
    </table>